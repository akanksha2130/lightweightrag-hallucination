# LightweightRAG — exact package versions used in all experiments
# Install: pip install -r requirements.txt

# Core ML
torch==2.2.0                   # CPU build — no CUDA required
transformers==4.40.0
sentence-transformers==2.7.0

# Retrieval
rank-bm25==0.2.2
faiss-cpu==1.8.0               # CPU FAISS — do NOT install faiss-gpu

# Datasets
datasets==2.19.0

# Statistics
scipy>=1.11.0
numpy>=1.24.0
